// Supabase removed. This placeholder stays to avoid import errors if any leftover references remain.
// Please migrate code to use REST endpoints and local DB via server routes.
export const isSupabaseConfigured = false;
export const supabase: any = null;
